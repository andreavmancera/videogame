class Recipe {
    constructor(ctx, x, y, type) {
        this.ctx = ctx;
        this.x = x;
        this.y = y;
        this.type = "type";

    }

    draw() {
        if (this.type === "tomato") {
            this.ctx.drawImage()
        }
    }


}